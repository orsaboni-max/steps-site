# 2026-09-07 — כתיבה מחדש של `pilates-postpartum.html` ממקורות רפואיים מצוטטים

**ענף:** `content/postpartum-rewrite` (מ-main 730bd28). **לא הועלה** — ממתין ל-RV2.

## למה
אור עצר ב-7/9: "אני מאמן כושר, לא מאמן פילאטיס מכשירים. צריך מחקר מעמיק ומשהו אמיתי, לא מומצא."
המחקר: `marketing/reports/postpartum-research-2026-09.md` — 21 טענות מהדף הישן נבדקו מול 9 מקורות
(משרד הבריאות ×3, ACOG ×2, NHS ×2, עלון פיזיותרפיה NHS Trust, סקירה שיטתית PMC, CDC). אור אישר להחליף (7/9).

## מה השתנה
- כל התוכן בין ה-hero ל"עוד בסטפס" הוחלף בנוסח סעיף ב של הדוח. כל טענה נושאת מספר מקור `[n]` שמקשר ל-`#src-n`.
- 4 טענות ירדו כי אין להן מקור או שהמקורות סותרים אותן: "אחרי שהרופאה אישרה — ולא לפני" כתנאי גורף ·
  "כפיפות בטן מחמירות בטן פתוחה" · "רוב הנשים מספרות על שיפור תוך כמה שבועות" · רלקסין / מרכז הכובד.
- הטבלה "מה כן ומה עדיף לדחות" חולקה לשלבים לפי המקורות (ימים ראשונים · 6–8 שבועות · בהמשך; ריצה ≥ 3 חודשים).
- מקטע "מקורות": 10 קישורים חיים (`target="_blank" rel="noopener"`, `dir="ltr"` לכותרות באנגלית). קישור "כללית" הוסר.
- FAQ: 9 שאלות, ה-FAQPage schema נוצר מה-HTML ולכן זהה מילה במילה. Article `dateModified` = 2026-09-07, author = Organization.
- הערה משפטית ב-CTA עודכנה: ייעוץ רופא נדרש אחרי קיסרי/סיבוכים/לפני עצימות גבוהה — לא כתנאי לכל תנועה.
- `sitemap.xml` lastmod → 2026-09-07.

## אימות
- 21 בדיקות ירוקות · 4 בלוקי JSON-LD נפרסים · כל 10 המקורות החזירו 200 (curl; CDC חוסם curl ואומת ב-WebFetch).
- 390 ו-1280 (Chromium דרך DevTools): אין גלישה אופקית (scrollWidth = viewport), CTA מעל הקפל (282px / 384px),
  הטבלה `overflow-x:auto` וגוללת בתוך עצמה, 0 קישורי-ציטוט שבורים, 0 שגיאות קונסול מהדף עצמו.
- מגבלה: חלון הדפדפן של הסשן היה מוסתר (viewport 0×0), לכן צילומי המקטעים הגלולים נלקחו ב-Chromium headless
  עם אמולציית מכשיר — לא בטאב גלוי. QA בעיניים של אור על הדומיין = חלק מ-RV2.

## מה לא נעשה
- לא deploy. לא נגעו בדפים אחרים.
- `.claude/launch.json` קיבל תצורה `static-8766` (פורט 8765 היה תפוס ע"י סשן אחר) — לא חלק מהקומיט.
