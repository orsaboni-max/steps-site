# תוכנית הצמיחה — סשני S1–S3 (7/9/2026)

מקור: `C:/Users/USER/.claude/plans/flickering-cuddling-catmull.md`. שלושת הסשנים רצו במקביל בענפים נפרדים ומוזגו ל-`integration/s1-s3`. ביקורת RV1 תיקנה: חלונות זמן בסכמת הילדים, רווח ב-H1 ילדים, מחלקה משותפת לפס האמון, טסטים ללא skip, כותרת דף התזונה.

## S1 — דפי השירות, 7/9/2026
- בוצע לפי התוכנית `C:\Users\USER\.claude\plans\flickering-cuddling-catmull.md` בענף `claude/quizzical-mayer-9dc2fc`, בשלושה קומיטים (`barre` / `gym` / `pilates`). לא פורסם — הפריסה היא באחריות RV1.
- בשלושת הדפים: בלוק זהות HealthClub אחד עם `@id`, כתובת ושעות אחידות ו-`priceRange` ₪230-₪530, לצד `Service` עם מחירים ו-`Person` למדריכה של הדף; פס אמון `4.8 ★ · 31 ביקורות בגוגל · מעל 500 מתאמנות` מקושר לרשימת הביקורות; ההמלצות עם השמות המומצאים הוחלפו בביקורות גוגל מילה במילה, בלי כוכבים לביקורת בודדת ובלי תפקיד; שורת "מסלול משולב" ליד המחירים; `data-track` על כל CTA.
- בר: H1 חזר לטקסט קריא (`אימון בר בנתניה`) ואנימציית האותיות ירדה; הכפתור מעל הקפל הוא קישור Arbox אמיתי; נוסף מקטע ביקורות וכרטיס טלי; קישור הביטולים החשוף קיבל "(הגעתי מהאתר)". GYM: 3 כרטיסי מחיר במקום פרוזה + כרטיס אור תורג׳מן; גודל קבוצה עדיין לא מתפרסם. פילאטיס: כרטיס אוריה, תת-שורה בכרטיסי המחיר והרחבת המדידה; H1 לא שונה.
- בדיקות: 17 בדיקות האתר והלידים עברו; כל קישורי `wa.me` נושאים את סימן הייחוס ואין `text=` כפול; ה-JSON-LD נטען; ב-390/768/1280 אין גלישה אופקית והכפתור הראשון מעל הקפל; לחיצה על הכפתורים משדרת `cta_trial_click` · `arbox_open` · `whatsapp_click`, ואין `Lead` בדפים האלה.
## S2 — מדריכים + דף הילדים — 7/9/2026
- לפי `C:\Users\USER\.claude\plans\flickering-cuddling-catmull.md` (סעיף 5, סשן S2). 4 המדריכים וקובץ הילדים: CTA מעל הקפל + פס אמון, בלוק זהות אחיד (HealthClub `@id`), byline "נכתב על ידי אור סבוני", קישורי "עוד בסטפס". `pilates-price.html` קיבל גם title/description/H1 לפי נספח B.
- `pilates-postpartum.html`: FAQ schema קיבל את שאלת "כמה זה עולה?" שהייתה חסרה, נוסף H2 "מתי אפשר להתחיל" עם תשובה ישירה (162 מילים), נוסחי "צריך/אסור" רוככו, ונוספו 2 מקורות שאומתו חיים ב-WebFetch (משרד הבריאות, כללית — ACOG נחסם ל-WebFetch, לא נוסף). **הניסוח הרפואי טרם אושר על ידי אור — לאשר לפני RV1.**
- `pilates-beginners.html:148` — תוקן רווח חסר לפני `<em>`.
- `kids.html`: הוחלף `SportsActivityLocation` בבלוק הזהות + 3 `Service` + `FAQPage` (7 שאלות, זהות מילה במילה לטקסט הגלוי). כפתור וואטסאפ צף חדש, כיתוב AI גלוי לתמונת הפתיח, "קניון רמת פולג"→"קניון פולג", "תיקון יציבה וגב"→"עבודה על יציבה וחיזוק", מדידה עברה למטפל delegated אחיד. `kids-hero.jpg`/`kids-posture.jpg` קיבלו גרסאות avif/webp (ffmpeg, 30–80KB) דרך `<picture>`.
- באג שנתפס ותוקן: כפתורי ה-CTA המלאים-ברוחב בכרטיסי הפעילות במובייל התנגשו עם הכפתור הצף החדש — צומצם הרוחב שלהם ב-68px כדי להשאיר מקום.
- טסטים: 4 חדשים ב-`tests/website.test.mjs` (זהות משותפת מול index, H1 בר, סימון "הגעתי מהאתר" בכל wa.me, עקביות מספר הביקורות). שניים מהם `skip` עד ש-S1/S3 (ענפים נפרדים, לא במיזוג הזה) יעדכנו את `index.html`/`barre.html`.
- אומת בדפדפן (390/768/1280, 5 הדפים): אין גלישה אופקית, 0 שגיאות קונסול, CTA מעל הקפל, פס אמון וכיתוב AI גלויים. לא בוצע deploy ולא push.


## S3 — בית, תזונה וקבצי מערכת — 7/9/2026
- ענף `claude/inspiring-bhaskara-9176d9`: ביקורות גוגל אמיתיות בבית, reviewCount 31, שורת מסלול משולב, הסרת "99 ₪" מהתזונה + שורת פתיח, robots.txt לבוטים של AI, llms.txt, הפניות ב-vercel.json, sitemap, תיקון שעות ב-directory-listing.md.

## S4 — תוכן — 7/9/2026
ענף `claude/s4-content` (מ-main), שלושה קומיטים: `content(barre)` · `content(price)` · `content(pages)`. לא פורסם — הפריסה היא באחריות RV2.

- **דף הבר** 816 → 1,874 מילים (נספר על גוף הדף, בלי פוטר; המדידה בתוכנית, 877, נעשתה בשיטה אחרת). תשעת ה-H2 של נספח H נבנו במלואם: תיבת "התשובה הקצרה" (138 מילים), פירוק השיעור לפי דקות, "למי מתאים ולמי פחות", טבלת בר/רפורמר/כוח, כמה פעמים בשבוע, מה לובשים, מקטע הסטודיו, "כמה עולה אימון בר בנתניה", ו-FAQ 5→8 — שמונה השאלות זהות מילה במילה ב-`FAQPage`. כל המקטעים הקיימים נשמרו (ביקורות, כרטיס טלי, לוח השעות, כל ה-CTA). **חריגה מהיעד ~1,300–1,500** — תשעה מקטעים עם תוכן אמיתי לא נכנסים ל-1,500.
- **המחירון** 1,177 → 1,643 מילים. בלוק "כמה עולה פילאטיס מכשירים בדרך כלל" מעל טבלת STEPS (3 מודלי תמחור + טווחים), מקטע "שיעור בודד או מנוי חודשי", "המיקום" נוסף כסעיף 5 ל"מה באמת קובע" במקום לשכפל אותו, ו-FAQ +1 ("כמה עולה פילאטיס מכשירים בישראל?") גם בסכמה.
- **טווחי המחירים הארציים אושרו רק בזכות 4 מקורות חיים שנמשכו ב-WebFetch ב-7/9/2026** — natalis, fit-studio, qilates (סטודיו שמיים וארץ), mgfitness — ומופיעים בשורת `.src-note` עם התאריך והקישורים. הטקסט אומר במפורש שרוב הסטודיואים לא מפרסמים מחירים ושאלה טווחים ולא מחירון.
- **פסקאות תשובה ל-AI** (125–142 מילים בעברית — צפיפות העברית נמוכה מהיעד האנגלי 134–167): פילאטיס אחרי פס האמון, GYM ב-H2 חדש "כמה עולה חדר כושר לנשים בנתניה", ילדים אחרי כותרת הפעילויות, תזונה מעל ה-stats (כולל השארת "מאמן כושר, לא דיאטן" גלוי).
- **שכונות** — "מגיעות אלינו מפולג, מרמת פולג, מעיר ימים ומדרום נתניה" פעם אחת בפילאטיס (FAQ החניה) ופעם אחת ב-GYM. בלי מרחקי הליכה ובלי זמני נסיעה.
- **CSS** — `.answer`/`.answer-t` הועתקו זהים מ-`content.css` לארבעת הדפים עם גיליון inline; `.tw`/`table.cmp`/`.c-list` נוספו לבר בלבד; `.src-note` נוסף ל-`content.css`.
- **אימות:** 21 בדיקות ירוקות · Playwright על 6 הדפים ב-390 וב-1280: `scrollWidth == clientWidth` בכל 12 הצירופים, הטבלאות גוללות בתוך עצמן (620/348 בבר, 545/348 במחירון), 0 שגיאות קונסול מלבד 404 של `/api/schedule` בשרת הסטטי · כל בלוקי ה-JSON-LD עוברים `JSON.parse`.
