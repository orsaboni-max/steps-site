# ⚖️ STEPS שיווק — חוזה העבודה (נטען אוטומטית בכל שיחה דרך hook)
> אלה הכללים שהמערכת מכריחה אותי להחיל — לא תלוי בזיכרון שלי.

## ✅ פרוטוקול האימות (הכלל #1 של Anthropic — ה"טסט" שלי)
> "תן ל-Claude דרך לאמת את עצמו." בשיווק אין `npm test` — **המספר מ-Meta/Arbox הוא הטסט.**
לפני **כל** קביעה, חובה למשוך את הנתון האמיתי ולצטט אותו:
- "הקמפיין עובד/לא" → משוך `cost_per_result` (₪/שיחה, ₪/ליד). **אסור CTR לבד.**
- "כך וכך לקוחות/הכנסה/נטישה" → משוך מ-Arbox, לא מהזיכרון.
- "המנצח הוא X" → משוך ביצוע חי, לא הנחה.
**אם לא משכתי נתון — אסור לי לקבוע. אומר "אני בודק", לא "כן".**

## 🔒 4 חוקי הברזל (אסור לסטות)
1. **שיפוט קמפיין = ₪ לתוצאה, לא CTR.** לפני שאומר "עובד/לא עובד" — חובה למשוך `cost_per_result` מ-Meta (שיחות וואטסאפ / לידים). CTR לבד = אסור כקביעה.
2. **שום כסף בלי "go".** השקה / שינוי תקציב / כיבוי / קריאייטיב לאוויר — רק באישור מפורש של אור.
3. **לקרוא לפני לדבר.** בתחילת עבודה שיווקית: `marketing/STEPS-CONTROL-CENTER.html` + `STATUS.md` + הסעיף הרלוונטי ב-`meta-playbook.md`.
4. **כל נתון אמיתי.** אסור placeholder/ניחוש שמתחזה לעובדה. לא בטוח → "אני בודק", לא "כן".

## 🔴 עבודת Meta Ads — הדרך היחידה (לקח 1/6 — לא לחזור על הפדיחה!)
- **העלאה/שינוי מודעה/קריאייטיב = Graph API ישיר בלבד.** טוקן: `C:\Users\USER\meta-token-temp.txt` (StepsAds, לא פג). graph.facebook.com/v21.0 → `/adimages`(hash) → `/adcreatives` → POST ל-ad. **לא MCP** (כתיבה חסומה ברולאאוט) **ולא דפדפן** (קופא). סקריפטים מוכנים ב-`marketing/creative/*/` (swap_creative / preview_collage / fix_collage_4x5).
- **קריאייטיב לפני "מוכן":** פיד = **4:5** (9:16/3:4 נחתכים!). safe-zone (לוגו+CTA ב-60% מרכזי). **preview רשמי (פיד+סטורי) → אימות אחרי ההעלאה.**
- **לפני שקופצים לכלי כלשהו — חקור איך עשינו בעבר** (היסטוריה/סקריפטים). פירוט מלא: `SOURCE_OF_TRUTH.md` §Meta Runbook.

## 🧠 משמעת זיכרון (אוטומטי — לא לחכות שאור יבקש)
**אחרי כל התקדמות משמעותית** (ממצא חדש, החלטה, בנייה, מספר חי שנמשך) — **עדכן מיד** את ה-snapshot:
`~/.claude/projects/C--Users-USER-steps-site/memory/project_steps_state_<תאריך>.md` + `marketing/STATUS.md` + הדשבורד הרלוונטי.
**לפני שמסיימים/עוצרים שיחה** — חובה לוודא שהמצב נשמר. אסור להסתמך על שאור יזכיר. זה מה שהופך אותי ממכונה ל"צ'אט שמסבירים לו מחדש".

## 🧰 הכלים שאני חייב להפעיל (לא להמציא מחדש)
| משימה | הסקיל/הכלי |
|---|---|
| ניתוח ביצועים | `steps-performance-analyst` + `meta-ads-analyzer` (Breakdown Effect, Learning Phase) |
| הקמת קמפיין | `steps-campaign-builder` |
| קופי | `steps-ad-copy` | קריאייטיב | `steps-reel-pipeline` / `steps-studio-visuals` |
| רעיונות/אסטרטגיה | `steps-creative-strategy` + `idea-dashboard.html` |
| נתוני Meta חיים | Meta MCP (חשבון 9773300439396945) — קריאה. כתיבה רק עם go. |
| נתוני סטודיו | Arbox MCP (branch=poleg) — לקוחות, churn, ייחוס |
| מקור אמת לכל נושא | `SOURCE_OF_TRUTH.md` |

## 📊 מספרי עוגן (בייסליין)
- ₪10.5 לשיחת וואטסאפ · ₪30 לליד-טופס · ₪389 ללקוחה משלמת/חודש · 484 משלמות · ~44 עוזבות/חודש.

## 🚫 לקחים נעולים (feedback — לא לחזור עליהם)
- FreeFit/MOOVE = אגרגטור, **לא** משפך גיוס מנויות.
- new_members ב-Arbox ≠ לקוחות חדשות (מערב חידושים).
- לא להבטיח מה שלא בדקתי. תת-הבטחה, על-ביצוע.
